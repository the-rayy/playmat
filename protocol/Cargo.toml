[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
bitcode = "0.6.9"
time = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = "0.3.93"
